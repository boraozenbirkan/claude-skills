{
  "permissions": {
    "ask": [
      "Edit(/docs/05-public/**)"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"Reminder: end this response with a Your turn section listing every question, decision, and action needed from the user, numbered and restated in full, even where already asked inline above. If nothing is needed, say so in one line.\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "if": "Edit(REPLACE_WITH_MIGRATIONS_GLOB)",
            "command": "echo '{\"systemMessage\": \"Routing: migrations map to docs/02-architecture/data-model.md. Update it in this change, before reporting the work complete.\"}'",
            "statusMessage": "Doc routing"
          },
          {
            "type": "command",
            "if": "Edit(REPLACE_WITH_AUTH_GLOB)",
            "command": "echo '{\"systemMessage\": \"Routing: auth and access rules map to docs/02-architecture/security.md. Update it in this change, and check docs/00-meta/deferred-ledger.md for security entries this change may have fired.\"}'",
            "statusMessage": "Doc routing"
          },
          {
            "type": "command",
            "if": "Edit(REPLACE_WITH_ENV_OR_CONFIG_GLOB)",
            "command": "echo '{\"systemMessage\": \"Routing: env vars and external services map to docs/02-architecture/integrations.md, and any new setup step to docs/04-development/local-setup.md. Update what applies in this change.\"}'",
            "statusMessage": "Doc routing"
          },
          {
            "type": "command",
            "if": "Edit(REPLACE_WITH_PUBLIC_ROUTES_GLOB)",
            "command": "echo '{\"systemMessage\": \"Routing: public routes map to docs/04-development/seo.md - check metadata, canonical URL, and sitemap. A new pipeline stage maps to docs/02-architecture/pipelines.md.\"}'",
            "statusMessage": "Doc routing"
          },
          {
            "type": "command",
            "if": "Edit(REPLACE_WITH_ADMIN_ROUTES_GLOB)",
            "command": "echo '{\"systemMessage\": \"Routing: admin and staff screens map to docs/03-internal/. Update the workflow page for any change to fields, statuses, or steps.\"}'",
            "statusMessage": "Doc routing"
          }
        ]
      }
    ]
  }
}
